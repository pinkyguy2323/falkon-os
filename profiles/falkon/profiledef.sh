#!/usr/bin/env bash
# Falkon OS - единый профиль: ОДНА система, три оформления (kde/hypr/cmd).
# Сам профиль берётся из штатного releng (см. scripts/build-iso.sh).
ISO_NAME="falkon"
ISO_LABEL="FALKON_$(date +%Y%m)"
ISO_PUBLISHER="Falkon OS <https://github.com/falkon-os>"
ISO_APPLICATION="Falkon OS - one system, three looks"
ISO_VERSION="$(date +%Y.%m.%d)"
